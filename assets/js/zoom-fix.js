/* ============================================================
   FEEDNOW — ZOOM NORMALIZATION
   Forces the page to always fit the viewport properly
   ============================================================ */

(function () {
  'use strict';

  // No zoom manipulation needed — handled via CSS viewport units
})();
